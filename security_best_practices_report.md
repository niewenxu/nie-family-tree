# 聂氏宗谱页面安全检查报告

检查日期：2026-07-25

## 执行摘要

本次检查覆盖密码保护、公开数据暴露、DOM 注入、浏览器安全策略、旧入口及验证交互。已修复所有发现的可直接整改问题：受保护资料已从公开 `data.json` 移出并采用 AES-256-GCM 加密；密码不再写入前端源码；动态数据在进入 HTML 前统一转义；页面增加内容安全策略；旧版入口改为最小跳转页；验证弹窗增加失败限速、临时会话说明、焦点管理及移动端适配。

仍需注意：主密码为四位数字，其抗离线猜测能力有限。PBKDF2 可提高猜测成本，但无法让弱密码具备强密码的安全性。后续建议改用长度至少 12 位的随机密码；若资料敏感度继续提高，应迁移至服务端身份验证与访问控制。

## 检查结果

### SEC-001（高）公开源码含明文密码，受保护资料可绕过界面直接读取

- 原位置：`app-fixed.js` 的两个验证函数；公开 `data.json`。
- 风险：任何访问者均可查看 JavaScript 得到密码，或直接下载 JSON 绕过弹窗读取出生日期、联系方式、简介和待考人员资料。
- 修复：新增 `protected-data.json`，使用 PBKDF2-SHA-256（310,000 次）派生密钥及 AES-256-GCM 加密；公开 JSON 只保留展示所需的脱敏姓名、记录键及出生日期是否已知的布尔标记。密码仅在浏览器内用于解密，未保存到源码、Cookie、`localStorage` 或 `sessionStorage`。
- 状态：已修复。

### SEC-002（中）动态族谱数据通过 `innerHTML` 拼接

- 原位置：`app-fixed.js` 中人物详情、辈分、世系和姓名渲染。
- 风险：若维护表或 JSON 被误植入 HTML/脚本片段，可能形成存储型 DOM XSS。
- 修复：新增统一 `escapeHTML()`，所有进入 HTML 模板的动态字段均先进行实体转义；可直接使用 DOM API 的清空操作改为 `replaceChildren()`。
- 状态：已修复。

### SEC-003（中）页面缺少浏览器内容安全策略

- 原位置：`new-page.html` 与旧 `index.html`。
- 风险：出现注入缺陷时，浏览器缺少第二道脚本、连接与对象加载限制。
- 修复：增加 CSP，限制脚本、图片、连接、表单、对象和基础 URL 的来源；增加 `no-referrer` 策略。因现有页面仍包含内联样式，`style-src` 暂保留 `'unsafe-inline'`，但脚本不允许内联执行。
- 状态：已修复。

### SEC-004（中）验证尝试无限制，弹窗可访问性不足

- 原位置：旧密码验证弹窗。
- 风险：可在界面中高速反复尝试；键盘焦点可能离开弹窗；关闭后焦点丢失；移动端操作区域偏小。
- 修复：每类密码连续失败 5 次后锁定 30 秒；验证期间禁用输入；加入显示/隐藏密码、明确错误反馈、焦点循环、关闭后焦点恢复、页面滚动锁定、44px 触控目标及手机底部弹层布局。
- 状态：已修复。界面限速不能阻止对下载后密文的离线猜测，仍建议更换强密码。

### SEC-005（中）根入口保留旧版重复页面及旧密码逻辑

- 原位置：`index.html`。
- 风险：即使主页面已修复，旧入口仍暴露旧逻辑和明文密码，并扩大维护与攻击面。
- 修复：将根入口替换为无脚本的最小跳转页，统一进入 `new-page.html`。
- 状态：已修复。

### SEC-006（低）数据加载存在跨域原始仓库回退

- 原位置：`app-fixed.js` 初始化数据源。
- 风险：扩大连接范围，并使部署站点的数据来源不再完全由同源版本控制。
- 修复：删除跨域回退，仅同源并行加载公开数据和加密数据；CSP 的 `connect-src 'self'` 同步强制执行。
- 状态：已修复。

## 发布前验证

- JavaScript 语法检查通过。
- 加密数据使用两组现有密码分别成功解密，主资料 102 条、世系待考 25 条。
- 公开数据中联系方式字段为 0、出生日期字段为 0、待考人员明文列表不存在。
- 已跟踪并发布的 HTML、JavaScript、JSON 文件未检出两组明文密码。
- CSP、无来源回退、键盘焦点与减少动态效果规则均已加入。
